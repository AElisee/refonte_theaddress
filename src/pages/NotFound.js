import React from "react";

const NotFound = () => {
  return (
    <div
      className="not-found"
      style={{
        height: "300px",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        color: "brown",
      }}
    >
      <h1>Page introuvable !!!</h1>
    </div>
  );
};

export default NotFound;
