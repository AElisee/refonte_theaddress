import React from "react";

const Loader = () => {
  return (
    <div
      className="loader"
      style={{ width: "100%", height: "100vh", color: "red" }}
    >
      <h1>Loading</h1>
    </div>
  );
};

export default Loader;
